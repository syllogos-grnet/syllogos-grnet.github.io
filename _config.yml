# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: 'Σύλλογος Εργαζομένων και Συνεργατών ΕΔΥΤΕ'
email: your-email@example.com
description: ''
baseurl: "" # the subpath of your site, e.g. /blog
url: 'https://syllogos.grnet.gr'

# Build settings
markdown: kramdown
theme: minima
plugins:
  # gh-pages always-on
  - jekyll-coffeescript
  - jekyll-default-layout
  - jekyll-gist
  - jekyll-github-metadata
  - jekyll-optional-front-matter
  - jekyll-paginate
  - jekyll-readme-index
  - jekyll-titles-from-headings
  - jekyll-relative-links
  # extra
  - jekyll-feed
  - jekyll-redirect-from
  - jekyll-sitemap

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

header_pages:
  - blog.md
  - engrafi-sto-syllogo.md
  - about/katastatiko-syllogou.md

lang: el
locale: el_GR

minima:
  date_format: "%-d %b %Y"

show_excerpts: true

# Hack: Use _includes/google-analytics.html for favicons, only to avoid
# overriding _includes/head.html; this is natively supported in minima HEAD,
# yet unreleased
google_analytics: true

contacts:
  - name: Διοικητικό Συμβούλιο Συλλόγου
    email: 'qf-flyybtbh@nqzva.tearg.te'
    email_protected: true
  - name: Ολομέλεια Συλλόγου
    email: flyybtbf@yvfgf.tearg.te
    email_protected: true
